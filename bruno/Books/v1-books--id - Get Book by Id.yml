info:
  name: /v1/books/:id - Get Book by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{http_url}}/v1/books/:id"
  params:
    - name: id
      value: "1000"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
