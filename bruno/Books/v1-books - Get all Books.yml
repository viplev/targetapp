info:
  name: /v1/books - Get all Books
  type: http
  seq: 1

http:
  method: GET
  url: "{{http_url}}/v1/books"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
