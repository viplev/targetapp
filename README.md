# Benchmark Target App

This app is the target app for the evaluation of the project.

The app includes a simple API, for a full CRUD operation of a single operation. 

The app also introduces an option for slowing down the application through an environment variable.
This is necessary to have to app to perform worse, and introduce an overhead in CPU usage.
